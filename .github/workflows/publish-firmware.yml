name: Publish Firmware

on:
  release:
    types: [published]

permissions:
  contents: write

jobs:
  build-firmware:
    name: Build Firmware
    uses: esphome/workflows/.github/workflows/build.yml@0fdd5e311b7e744069166696072a1a9cbc5fbeb6  # 2026.8.1
    with:
      #### Modify below here to match your project ####
      # files: |
      #   reward-dispenser-esp32.factory.yaml
      #   reward-dispenser-esp32-c3.factory.yaml
      #   reward-dispenser-esp32-c6.factory.yaml
      #   reward-dispenser-esp32-s3.factory.yaml
      files: |
        reward-dispenser-esp32-c3.factory.yaml
      esphome-version: 2026.6.5
      combined-name: reward-dispenser
      #### Modify above here to match your project ####

      release-summary: ${{ github.event.release.body }}
      release-url: ${{ github.event.release.html_url }}
      release-version: ${{ github.event.release.tag_name }}

  upload-to-release:
    name: Upload to Release
    uses: esphome/workflows/.github/workflows/upload-to-gh-release.yml@0fdd5e311b7e744069166696072a1a9cbc5fbeb6  # 2026.8.1
    needs:
      - build-firmware
    with:
      version: ${{ github.event.release.tag_name }}
